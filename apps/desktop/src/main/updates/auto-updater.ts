import { app } from 'electron';

export class AutoUpdater {
  constructor() {
    // Placeholder: configure electron-updater
  }

  async checkForUpdates(): Promise<void> {
    // Placeholder: check GitHub Releases
  }
}
